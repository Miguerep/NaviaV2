## Nota

Este repo no es un proyecto web (es Flutter + FastAPI), pero se crea esta carpeta para mantener las integraciones externas **aisladas** si en el futuro se añade un frontend web.

Integraciones añadidas hoy:
- Backend: `backend/app/features/api/*`
- Flutter: `app/lib/components/external/*`

